You can use the smaller elements to reduce the number of binary bits.
That means you can sort arrays in increasing order and take a_1, a_2,..., a_m as the final sequence.
So, you only need to check if m * len(a_m) ≤ b.